using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.Text;
using System.Text;

namespace REslava.Result.SourceGenerators
{
    [Generator]
    public class SimpleTestGenerator : IIncrementalGenerator
    {
        public void Initialize(IncrementalGeneratorInitializationContext context)
        {
            // TODO: DEBUG - Remove after generator testing is complete
// PURPOSE: Debug output to verify SimpleTestGenerator initialization
// CONTEXT: Testing basic source generator functionality
// CLEANUP: Remove after generator infrastructure is stable
// ASSIGNED: Development Team
// DUE: Generator Infrastructure Complete
System.Diagnostics.Debug.WriteLine("🚀 SimpleTestGenerator.Initialize() - GENERATOR LOADED!");
// END-TODO
            
            // Generate code for ALL compilations - no attribute required
            context.RegisterSourceOutput(context.CompilationProvider, (spc, compilation) =>
            {
                // TODO: DEBUG - Remove after generator testing is complete
// PURPOSE: Debug output to verify SimpleTestGenerator code generation
// CONTEXT: Testing basic source generator functionality
// CLEANUP: Remove after generator infrastructure is stable
// ASSIGNED: Development Team
// DUE: Generator Infrastructure Complete
System.Diagnostics.Debug.WriteLine($"🔥 SimpleTestGenerator generating code for {compilation.AssemblyName}");
// END-TODO
                
                var source = @"
// Generated by SimpleTestGenerator - ALWAYS GENERATED
namespace Generated.Simple
{
    public static class TestExtensions 
    {
        public static string TestMethod(this string input) 
        {
            return input + "" - SIMPLE_GENERATOR_WORKS!"";
        }
    }
}";
                
                spc.AddSource("SimpleTestExtensions.g.cs", SourceText.From(source, Encoding.UTF8));
            });
        }
    }
}
