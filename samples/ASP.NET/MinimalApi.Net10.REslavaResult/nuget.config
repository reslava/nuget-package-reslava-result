<?xml version="1.0" encoding="utf-8"?>
<!-- TODO: NUGET CONFIGURATION - Review before production release
     PURPOSE: Track NuGet package source configuration for OneOf<T1,T2,T3> project
     CONTEXT: Ensures consistent package sources for development and production
     CLEANUP: Consider adding internal package sources for REslava.Result packages
     ASSIGNED: Development Team
     DUE: Production Release
-->
<configuration>
  <!-- TODO: PACKAGE SOURCES - Validate for production environment
       PURPOSE: Define NuGet package sources for OneOf integration dependencies
       CONTEXT: Currently uses public NuGet.org, may need internal sources
       CLEANUP: Add internal package feed when REslava.Result packages are published
       ASSIGNED: Development Team
       DUE: REslava.Result Package Publication
  -->
  <packageSources>
    <!-- TODO: CLEAR SOURCES - Ensure clean package source configuration
         PURPOSE: Remove any inherited package sources for consistency
         CONTEXT: Prevents conflicts with machine-level package sources
         CLEANUP: Consider if internal sources should be added here
         ASSIGNED: Development Team
         DUE: Production Environment Setup
    -->
    <clear />
    
    <!-- TODO: PUBLIC NUGET SOURCE - Primary package source
         PURPOSE: Public NuGet.org source for external dependencies
         CONTEXT: Used for OneOf external package during development
         CLEANUP: Will be replaced by internal REslava.Result packages
         ASSIGNED: Development Team
         DUE: REslava.Result OneOf Library Release
    -->
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
    
    <!-- TODO: INTERNAL PACKAGE SOURCE - Add when REslava.Result packages are ready
         PURPOSE: Internal package feed for REslava.Result NuGet packages
         CONTEXT: Will host self-contained REslava.Result OneOf implementation
         CLEANUP: Uncomment and configure when internal package feed is available
         ASSIGNED: Development Team
         DUE: REslava.Result Package Publication
    -->
    <!-- <add key="reslava-internal" value="https://packages.reslava.com/v3/index.json" /> -->
  </packageSources>
</configuration>
